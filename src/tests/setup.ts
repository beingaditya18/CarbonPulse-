/**
 * @fileoverview Minimal Vitest setup file.
 * Declares any global hook configurations or test suite initializations.
 */

import { afterEach } from 'vitest';

afterEach(() => {
  // Clear or reset any global mocks/side effects here
});
